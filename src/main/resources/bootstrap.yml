spring:
  profiles: default
  application:
    name: test-manager-service
server:
  port: 8093
mybatis:
  mapperLocations: classpath*:/mapper/*.xml
  configuration:
    mapUnderscoreToCamelCase: true
management:
  port: 8101
  security:
    enabled: false
feign:
  hystrix:
    enabled: true
security:
  basic:
    enabled: false
  ignored: /v2/choerodon/api-docs

swagger:
    basepackage: io.choerodon #扫描api包路径